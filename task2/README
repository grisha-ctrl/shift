В первый аргумент передаётся путь к файлу, который хотим прочитать.
Во второй аргумент выбираем куда хотим записать результат (console - для вывода результата в консоль. file - для записи в файл).
При выборе вывода результата в файл нужно указать третий аргумент, который задаёт имя файла.